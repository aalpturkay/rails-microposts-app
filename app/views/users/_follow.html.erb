<%= form_with(model: current_user.active_relationships.build, local: true) do |f| %>
    <div><%= hidden_field_tag :followed_id, @user.id %></div>
    <%= f.submit "Follow", class: "cursor-pointer rounded-full border duration-500 bg-black py-2.5 px-3 text-sm font-bold text-white" %>
<% end %>
